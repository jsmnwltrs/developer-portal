import PropTypes from 'prop-types';

const tabDataShape = PropTypes.shape({
  name: PropTypes.string,
  url: PropTypes.string,
  uid: PropTypes.string,
  id: PropTypes.string,
});

export default tabDataShape;
